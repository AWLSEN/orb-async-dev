{
  "name": "orb-async-dev",
  "version": "0.0.1",
  "description": "Self-hosted async coding agent on Orb Cloud. Text your repo, come back to a PR.",
  "type": "module",
  "private": true,
  "bin": {
    "orb-async-dev": "./cli/init.ts"
  },
  "scripts": {
    "deploy": "bun run deploy/deploy.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@octokit/rest": "^21.0.0",
    "@octokit/webhooks": "^13.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nextbysam/orb-async-dev"
  },
  "license": "MIT"
}
