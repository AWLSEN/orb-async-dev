#!/usr/bin/env bash
# Binary-mode entry point Orb invokes. Bun is on /usr/local/bin from the
# build step (symlinked from /root/.bun) so it works whether the agent runs
# as root or a non-root user.
set -euo pipefail
exec bun run agent/orchestrator.ts
