orb-async-dev — build log
==========================

Batch 1 — scaffold + deploy to Orb (DONE)
  [x] commit 1: scaffold (README, LICENSE, package.json, tsconfig, .env.example, learnings.txt)
  [x] commit 2: deploy/orb-api.ts — wraps api.orbcloud.dev endpoints (9 tests)
  [x] commit 3: deploy/orb-toml.ts — autogenerator + template (13 tests)
  [x] commit 4: deploy/deploy.ts + state.ts — end-to-end idempotent provisioning (16 tests)
  [x] commit 5: agent/orchestrator.ts + smoke.sh — /health returns 200 (4 tests)
  Totals: 42/42 tests green, typecheck clean under strict+exactOptionalPropertyTypes.

Batch 2 — GitHub adapter (DONE)
  [x] commit 6: adapters/webhook-verify.ts — HMAC-SHA256 X-Hub-Signature-256 (8 tests)
  [x] commit 7: adapters/mention-parser.ts — @orb parser (17 tests, code-block/quote-line aware)
  [x] commit 8: adapters/event-router.ts — issues/issue_comment/pr_review_comment (11 tests)
  [x] commit 9: adapters/github-client.ts — default_branch, branches, PRs, comments (14 tests)
  [x] commit 10: orchestrator /github/webhook fully wired (verify + route + dedup + ack + enqueue) (8 new orchestrator tests)
  Totals: 100/100 tests green, typecheck clean.
  Repo: https://github.com/nextbysam/orb-async-dev (private)
  Demo target: https://github.com/nextbysam/orb-async-dev-demo (private, for dogfood)

Batch 3 — orchestrator + per-task sub-agent (DONE)
  [x] commit 11: agent/worktree.ts + agent/runner.ts — per-task git worktrees (8 tests)
  [x] commit 12: agent/tools/fs-tools.ts — jailed read/write/edit/list/run_bash (17 tests)
  [x] commit 13: deploy: align OrbClient + deploy.ts with current docs.orbcloud.dev
       (fixed 3 spec bugs: promote/demote port body, usage query params, startAgent response shape)
  [x] commit 14: agent/sub-agent.ts — Claude tool-use loop (6 tests)
  [x] commit 15: agent/task-runner.ts — webhook→PR end-to-end + wiring in orchestrator (11 tests)
  Totals: 145/145 tests green, typecheck clean.

Batch 4 — 9-gate verifier (DONE)
  [x] commit 16: agent/verifier/stack-detect.ts — bun/pnpm/yarn/npm/python/go/rust (8 tests)
  [x] commit 17: agent/verifier/types + shell-gates — build/test/lint/typecheck/scope (13 tests)
  [x] commit 18: agent/verifier/diff-gates — new_tests + mutation check (9 tests)
  [x] commit 19: agent/verifier/llm-gates — self_review + red_team (Claude-backed, 12 tests)
  [x] commit 20: agent/verifier/secret-gate + index + TaskRunner integration (19 tests)
  Totals: 208/208 tests green, typecheck clean.
  Verifier chain order: scope → secret_scan → new_tests → build → tests → lint → typecheck → mutation → self_review → red_team.
  Hard failures block the PR entirely (no push, no open). Soft failures (lint) become warnings in the PR body.

Batch 5 — health loops + cost watchdog + reaper (DONE)
  [x] commit 21: agent/health/scheduler.ts — interval scheduler w/ jitter + injectable clock (6 tests)
  [x] commit 22: agent/health/canary.ts — clone+status+detect+test probe + runCanaryAndNotify (7 tests)
  [x] commit 23: agent/health/cost-watchdog.ts — /v1/usage poll + sticky trip + once-only notify (8 tests)
  [x] commit 24: agent/health/task-registry.ts — in-memory registry + reapStuckTasks sweep (5 tests)
  [x] commit 25: orchestrator bootstrap wires all four into scheduler (canary 1h, cost 10m, reaper 5m)
       + TaskRunner.registry registration + isCostTripped gate rejects new tasks while tripped.
  Totals: 235/235 tests green, typecheck clean.

Batch 6 — launch polish (DONE)
  [x] commit 26: cli/init.ts — npx orb-async-dev init 4-question wizard (8 tests)
  [x] commit 27: agent/log-store.ts + /logs HTML dashboard auto-refresh (10 tests)
  [x] commit 28: docs — pitch-first README + CLAUDE.md onboarding
  [x] commit 29: launch.md — tweet drafts, demo gif script, post-launch backlog
  [x] commit 30: flip repo visibility public + final polish
  Totals (final): 253/253 tests green, typecheck clean.

Deploy to production — first live deploy (DONE)
  Live URL:   https://99cc1574.orbcloud.dev
  Repo:       https://github.com/AWLSEN/orb-async-dev (public)
  Demo repo:  https://github.com/AWLSEN/orb-async-dev-demo (private)
  Computer:   99cc1574-639d-4a4d-bf57-f2a5693a0d93 on api.orbcloud.dev

  Gotchas hit + fixed (in order):
    1. [resources] takes runtime_mb/disk_mb as ints, NOT "2GB" strings.
    2. lang must be node|python|go|rust|binary — no "typescript".
       Fix: lang=binary + bin/start wrapper that execs `bun run ...`.
    3. bun curl installer needs unzip — apt-get install before.
    4. Build runs as root (/root/.bun), agent runs as different user
       (/home/agent). Symlinked /root/.bun/bin/bun -> /usr/local/bin.
    5. Subsequent POST /build re-runs steps but does NOT re-clone the
       repo. Added `git fetch + reset --hard origin/<branch>` as first
       build step so updates actually roll.
    6. Orb RESERVES the env var ORB_PORT for internal routing (saw
       20198 at runtime). Renamed my env to HTTP_PORT; ORB_PORT remains
       the local operator-side render input.
    7. Agent stderr_tail often empty; /agent/data/orchestrator.log +
       tee in bin/start is the reliable debug surface (readable via
       GET /v1/computers/{id}/files/agent/data/orchestrator.log).

  Smoke: /health -> 200 ok; /logs -> HTML dashboard; /github/webhook
  without signature -> 401 missing_header; canary visible in /logs
  after ~35s (background scheduler running).

Design decisions locked in batch 1:
  - Runtime: Bun + TypeScript (fast, single binary, built-in test runner).
  - LLM: Anthropic via ANTHROPIC_API_KEY or ANTHROPIC_AUTH_TOKEN+BASE_URL proxy.
  - State: .orb-state/ stores computer-id, agent-id, api-key. Gitignored.
  - Deploy idempotency: every step checks existing state before hitting the API.
  - Surface: GitHub comments only for v0 (webhook → Orb URL). Telegram in v2.
  - Verification: 9-gate chain before any PR opens (see Batch 4).
  - Parallelism: N sub-agents per repo on git worktrees, isolated.
  - Zero idle cost: Orb checkpoints to NVMe; wake on webhook ~1s.
